# TorchGraph

This is an adaptor for old project names (`torchgraph`). We now changed the name to `entangle`, but there are some pickle objects that still refer to `torchgraph`. Thus, we keep this package.
